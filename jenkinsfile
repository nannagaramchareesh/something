pipeline {
    agent any
  
    stages {
        stage('Verify') {
            steps {
                script {
                    echo 'Verifying deployment...'
                    bat 'kubectl get pods'
                }
            }
        }
    }
}
