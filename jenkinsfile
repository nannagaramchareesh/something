pipeline {
    agent any
    
        stage('Verify') {
            steps {
                script {
                    echo 'Verifying deployment...'
                    bat 'kubectl get pods'
                }
            }
        }
    }
}
